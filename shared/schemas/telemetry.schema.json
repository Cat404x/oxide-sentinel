{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "TelemetryEnvelope",
  "description": "JSON envelope emitted by every Oxide Sentinel collector",
  "type": "object",
  "required": ["collector", "timestamp", "data"],
  "properties": {
    "collector": {
      "type": "string",
      "description": "Unique name of the collector that produced this record"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 UTC timestamp of when the telemetry was collected"
    },
    "data": {
      "type": "object",
      "description": "Collector-specific telemetry payload"
    }
  },
  "additionalProperties": false
}
